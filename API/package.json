{
  "name": "api",
  "version": "1.0.0",
  "description": "**API basics:** ``` Production mode: npm start Dev mode : npm run dev (only once, after DB is online, just type 'rs' in the terminal to restart the API) Run tests: npm test (after one of the above) ```",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "helmet": "^3.21.2",
    "lodash": "^4.17.15",
    "morgan": "^1.9.1",
    "nodemon": "^2.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "DEBUG=index,routes:movies nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
